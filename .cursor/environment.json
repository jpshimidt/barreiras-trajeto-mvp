{
  "install": "pip3 install --user -r requirements.txt pytest",
  "terminals": [
    {
      "name": "streamlit",
      "command": "export PATH=\"$HOME/.local/bin:$PATH\" && streamlit run app.py --server.headless true --server.port 8501"
    }
  ]
}
